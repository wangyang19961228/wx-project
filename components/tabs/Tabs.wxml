<view class="tabs">
    <!--标题-->
    <view class="tabs_title {{item.isActive?'active':''}}" 
          wx:for="{{tab}}" 
          wx:key="id"
          bind:tap="handleItemTap"
          data-index="{{index}}"
          >{{item.value}}</view>
</view>